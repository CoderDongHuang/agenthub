[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "agenthub-sdk"
version = "0.1.0"
description = "AgentHub developer gateway, tool package and MCP SDK"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["agenthub_sdk*"]
